# Build artifacts
_build/
/otelcol-gatewayapi
*.out
*.test

# Go module cache / vendor
/vendor/

# Editor detritus
.idea/
.vscode/
*.swp

# Local env
.env

# Token-bearing secret manifests must NEVER be committed. The naming
# convention is enforced here as a backstop — anyone preparing a
# real Secret should use one of these filename patterns. See
# deploy/k8s/dynatrace/README.md for the secret-materialization
# runbook (commit only sealed-secrets, not raw Secret YAMLs).
*WITH-TOKEN*
*-with-token*
*.secret.yaml
deploy/k8s/dynatrace/secret-dt-otlp-ingest.yaml
!deploy/k8s/dynatrace/secret-dt-otlp-ingest.example.yaml
!deploy/k8s/dynatrace/sealedsecret-dt-otlp-ingest.template.yaml
